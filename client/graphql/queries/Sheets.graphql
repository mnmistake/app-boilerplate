query sheetsQuery {
    sheets {
        id
        name
        createdAt
        user {
            id
            username
        }
    }
}

query sheetQuery($id: Int!) {
    sheet(id: $id) {
        name
        user {
            username
        }
        segments {
            id
            label
            content
            createdAt
        }
    }
}